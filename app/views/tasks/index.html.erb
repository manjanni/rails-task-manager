<h1>My Task List</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task)%> <%= task.completed %></li>
  <% end %>
</ul>


<p><%= link_to 'Add Task', task_new_path %></p>
